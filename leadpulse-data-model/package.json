{
  "name": "leadpulse-data-model",
  "version": "1.0.0",
  "private": true,
  "description": "Sequelize models, migrations, and seeders for LeadPulse. Consumed by leadpulse-api as a workspace dependency.",
  "main": "index.js",
  "scripts": {
    "migrate": "sequelize-cli db:migrate",
    "migrate:undo": "sequelize-cli db:migrate:undo",
    "migrate:undo:all": "sequelize-cli db:migrate:undo:all",
    "migrate:status": "sequelize-cli db:migrate:status"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1130.0",
    "@aws-sdk/s3-request-presigner": "^3.1131.0",
    "dotenv": "^16.4.5",
    "pg": "^8.11.5",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
